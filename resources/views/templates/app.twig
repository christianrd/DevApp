<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>{{ title }} | DevApp</title>

    <!-- Bootstrap -->
    <link href="{{ base_url() }}/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ base_url() }}/css/font-awesome.min.css">
    <!-- Main.css is a local STYLE -->
    <link rel="stylesheet" href="{{ base_url() }}/css/main.css">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <script src="{{ base_url() }}/js/vendor/jquery-1.11.2.min.js"></script>
</head>
<body>
    {% include 'templates/partials/navigation.twig' %}

    <div class="container">
        {% block content %} {% endblock %}
    </div>

    <footer class="footer">
        <div class="container">
            <p class="text-center text-muted">This <strong>FRAMEWORK</strong> was powered with <i class="fa fa-heart"></i> by <a href="https://www.christiandr.com">{{name}}</a></p>
        </div>
    </footer>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    {#<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>#}
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="{{ base_url() }}/js/vendor/bootstrap.min.js"></script>
    {% if title == 'Home' %}
        {% include 'templates/partials/mainjs.twig' %}
    {% endif %}
</body>
</html>